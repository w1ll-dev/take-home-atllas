import { ImageDimensions } from "@assets/images";

const iconButtonHitSlop = {
  bottom: ImageDimensions.close.height,
  top: ImageDimensions.close.height,
  lef: ImageDimensions.close.height,
  right: ImageDimensions.close.height,
};

export { iconButtonHitSlop };
